--- src/tests/transformers/gnu_standardizer/test_gnu_standardizer.h
+++ src/tests/transformers/gnu_standardizer/test_gnu_standardizer.h
@@ -698,6 +698,21 @@ TEST test_gnu_standardizer_zero_length_arrays(void) {
   PASS();
 }
 
+TEST test_gnu_standardizer_range_init(void) {
+  cdd_cst_tree_t *tree = NULL;
+  const char *code = "int arr[] = { [0 ... 2] = 5 };\n";
+  char *out = NULL;
+  cdd_transform_config_t config;
+  memset(&config, 0, sizeof(config));
+  ASSERT_EQ(0, cdd_cst_parse(az_span_create_from_str((char *)code), &tree));
+  ASSERT_EQ(0, cdd_transform_gnu(tree, &config));
+  ASSERT_EQ(0, cdd_cst_emit(tree, &out));
+  ASSERT(strstr(out, "[0] = 5, [1] = 5, [2] = 5") != NULL);
+  free(out);
+  cdd_cst_tree_free(tree);
+  PASS();
+}
+
 SUITE(transformer_gnu_standardizer_suite) {
   RUN_TEST(test_cdd_transform_gnu);
   RUN_TEST(test_gnu_standardizer_stmt_expr);
@@ -730,6 +745,7 @@ SUITE(transformer_gnu_standardizer_suite) {
   RUN_TEST(test_gnu_standardizer_float_extensions);
   RUN_TEST(test_gnu_standardizer_lvalue_cast);
   RUN_TEST(test_gnu_standardizer_zero_length_arrays);
+  RUN_TEST(test_gnu_standardizer_range_init);
 }
 #ifdef __cplusplus
 }
