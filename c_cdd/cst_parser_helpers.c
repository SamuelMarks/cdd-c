#include "cst_parser_helpers.h"

int dummy(void) { return 1; }

size_t eatFunction(struct tokenizer_az_span_element **arr,
                   size_t start_index,
                   size_t first_lbrace,
                   struct tokenizer_az_span_elem ***cur,
                   struct tokenizer_az_span_list *ll) {
  return 0;
}
